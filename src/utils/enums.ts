export enum roleType {
  'ADMIN' = 'ADMIN',
  'USER' = 'USER',
}

export enum VehicleType {
  'CAR' = 'CAR',
  'BIKE' = 'BIKE',
}

export enum Status {
  'AVAILABLE' = 'Available',
  'OCCUPIED' = 'Occupied',
  'RESERVED' = 'Reserved',
  'PENDING' = 'Pending',
}

// approved rejected pending disabled

export enum DurationType {
  'HOURS' = 'HOURS',
  'DAYS' = 'DAYS',
}

export enum ParkingStatusType {
  'APPROVED' = 'approved',
  'REJECTED' = 'rejected',
  'DISABLED' = 'disabled',
  'PENDING' = 'pending',
}
